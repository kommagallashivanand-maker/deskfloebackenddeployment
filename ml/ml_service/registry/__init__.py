"""
registry — DF-042

File-based model registry for the DeskFlow ML Service.

Public surface
--------------
    from registry.loader import load_model, preload_active_models
    from registry.registry import ModelRegistry, RegistryError
"""
